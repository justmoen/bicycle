{% extends 'base.html.twig' %}

{% block body %}
    <div><input type="button" value="Home" onclick="location.href = '{{ path('home_page') }}';" /></div>
    <div>
        <h2>Build {{ bicycleType }} Bicycle</h2>
    </div>
    {{ form_start(form) }}
    {{ form_widget(form.submit) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {% include 'bicycle/list.html.twig' %}
{% endblock %}